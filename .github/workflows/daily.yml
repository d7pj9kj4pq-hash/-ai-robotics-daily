name: AI日报生成

on:
  schedule:
    - cron: '0 1 * * *'  # 每天北京时间9点
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v3
      with:
        # 拉取所有分支和历史
        fetch-depth: 0
    
    - name: 设置Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install requests feedparser pyyaml
    
    - name: 创建必要目录
      run: |
        mkdir -p output/daily output/weekly output/images docs/daily
    
    - name: 运行新闻收集
      run: python scripts/news_collector.py
    
    - name: 运行AI处理
      env:
        ZHIPU_API_KEY: ${{ secrets.ZHIPU_API_KEY }}
      run: python scripts/ai_processor.py
    
    - name: 生成报告
      run: python scripts/report_generator.py
    
    - name: 生成图片
      run: python scripts/image_generator.py || echo "图片生成跳过"
    
    - name: 自动提交和推送
      uses: stefanzweifel/git-auto-commit-action@v4
      with:
        commit_message: "🤖 自动更新: $(date '+%Y-%m-%d') AI机器人日报"
        commit_options: '--allow-empty'
        file_pattern: 'output/* docs/*'
        commit_user_name: 'github-actions[bot]'
        commit_user_email: 'github-actions[bot]@users.noreply.github.com'
